package handlers

// Admin routes are handled via UserHandler, RoomHandler, and BookingHandler.
// This file exists as the admin handler entry point and can be extended
// with admin-specific aggregation endpoints.
